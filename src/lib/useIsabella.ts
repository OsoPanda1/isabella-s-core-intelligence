/**
 * useIsabella — Wrapper de compatibilidad (DEPRECATED).
 *
 * Este hook está deprecado. Utiliza useIsabellaAgent() directamente.
 * Se mantiene únicamente para compatibilidad con consumidores existentes.
 *
 * El motor unificado es useIsabellaAgent → IsabellaPipeline → C.R.O.W.N.
 */

export { useIsabellaAgent as useIsabella } from "./useIsabellaAgent";
